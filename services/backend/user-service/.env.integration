# Environment variables for Docker Integration Testing
NODE_ENV=integration
LOG_LEVEL=info

# Database Configuration for Docker
DB_HOST=postgres-users
DB_PORT=5432
DB_NAME=fantacontratti_users
DB_USER=postgres
DB_PASSWORD=password123

# Kafka Configuration
KAFKA_BROKERS=kafka:9092

# JWT Configuration  
JWT_SECRET=integration-jwt-secret-key
JWT_REFRESH_SECRET=integration-refresh-secret-key
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379